/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package com.gdn.scm.bolivia.repository;

import com.gdn.scm.bolivia.entity.LogisticProvider;
import java.math.BigDecimal;
import org.junit.*;
import org.junit.runner.*;
import org.springframework.boot.test.autoconfigure.orm.jpa.*;

import static org.assertj.core.api.Assertions.*;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.junit4.SpringRunner;

import org.springframework.boot.test.autoconfigure.orm.jpa.*;
import org.springframework.boot.test.autoconfigure.orm.jpa.AutoConfigureTestDatabase.Replace;
/**
 *
 * @author sofri
 */
//@RunWith(SpringRunner.class)
//@DataJpaTest
public class LogisticProviderRepositoryTest {
//    @Autowired
//    private TestEntityManager entityManager;
//
//    @Autowired
//    private LogisticProviderRepository repository;
//
//    @Test
//    public void testExample() throws Exception {
//        this.entityManager.persist(new LogisticProvider("kode", "a logistic ","Active",new BigDecimal(10),new BigDecimal(7)));
//        LogisticProvider logistic = this.repository.findByLogisticName("a logistic");
//        assertThat(logistic.getLogisticCode()).isEqualTo("kode");
//        assertThat(logistic.getStatus()).isEqualTo("Active");
//        assertThat(logistic.getDiscount()).isEqualTo(new BigDecimal(10));
//        assertThat(logistic.getVat()).isEqualTo(new BigDecimal(7));
//    }
}
